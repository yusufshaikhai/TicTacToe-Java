package strategy;

import models.Board;
import models.Cell;

public class MediumPlayingStrategy implements BotPlayingStrategy {
    @Override
    public Cell makeMove(Board board) {
        return null;
    }
}
